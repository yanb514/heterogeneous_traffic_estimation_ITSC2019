% function [send_flow] = sending_greenshield(rho, model)
%    rho_c = model.rm/2;
%    if rho <= rho_c
%        send_flow = greenshield(rho,model);
%    else
%        send_flow = model.vm * model.rm/4;
%    end
% end
